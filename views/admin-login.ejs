<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin Login</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      /* General body styling */
      body {
        background: linear-gradient(135deg, #e9f5ec, #b5d8b5); /* Soft green gradient */
        font-family: Arial, sans-serif;
        display: flex;
        justify-content: center;
        align-items: center;
        height: 100vh;
        margin: 0;
      }

      /* Form container */
      .container {
        max-width: 400px;
        background-color: #ffffff;
        padding: 30px;
        border-radius: 10px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
      }

      /* Heading styling */
      h2 {
        text-align: center;
        color: #155724; /* Dark green */
        margin-bottom: 20px;
        font-weight: bold;
      }

      /* Input fields */
      .form-control {
        border: 1px solid #b5d8b5; /* Light green border */
        border-radius: 8px;
        padding: 10px;
      }

      .form-control:focus {
        border-color: #28a745; /* Green highlight on focus */
        outline: none;
        box-shadow: 0 0 5px rgba(40, 167, 69, 0.5);
      }

      /* Button styling */
      .btn-primary {
        background-color: #28a745; /* Green button */
        border: none;
        border-radius: 50px;
        font-size: 16px;
        padding: 10px 20px;
        font-weight: bold;
        transition: background-color 0.3s ease;
      }

      .btn-primary:hover {
        background-color: #218838; /* Darker green on hover */
      }

      /* Link styling */
      .btn-link {
        color: #007bff;
        text-decoration: none;
        font-weight: bold;
        font-size: 14px;
      }

      .btn-link:hover {
        color: #0056b3; /* Darker blue on hover */
        text-decoration: underline;
      }

      /* Alert messages */
      .alert {
        color: #721c24; /* Red text for errors */
        background-color: #f8d7da; /* Light red background */
        border-color: #f5c6cb;
        border-radius: 5px;
        padding: 10px;
        font-size: 14px;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h2>Admin Login</h2>
      <form action="/admin-login" method="post">
        <!-- Display error messages -->
        <% if (invalidAdminCredentials) { %>
        <div class="alert text-center">
          Invalid credentials, please try again
        </div>
        <% } %>

        <!-- Email input -->
        <div class="mb-3">
          <label for="email" class="form-label">Email</label>
          <input
            type="email"
            class="form-control"
            id="email"
            name="email"
            placeholder="Enter admin email"
            required
          />
        </div>

        <!-- Password input -->
        <div class="mb-3">
          <label for="password" class="form-label">Password</label>
          <input
            type="password"
            class="form-control"
            id="password"
            name="password"
            placeholder="Enter admin password"
            required
          />
        </div>

        <!-- Submit button -->
        <button type="submit" class="btn btn-primary w-100">Login</button>

        <!-- Link to user login -->
        <div class="text-center mt-4">
          <a href="/login" class="btn btn-link">User Login</a>
        </div>
      </form>
    </div>
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
